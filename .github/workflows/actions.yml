name: Github Actions로 테스트 자동화하기
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Github Repository 파일 불러오기
        uses: actions/checkout@v4

      - name: Node.js 설치
        run: echo "첫 번째 작업의 두 번째 단계"

  second_job_job:
    runs-on: ubuntu-latest
    steps:
      - name: Github Repository 파일 불러오기
        uses: actions/checkout@v4

      - name: Node.js 설치
        run: echo "첫 번째 작업의 두 번째 단계"
